import { render, screen } from "@testing-library/react";
import App from "./App";

// test("renderiza algo da tela", () => {
//   render(<App />);
//   expect(screen.getByText(/hello/i)).toBeInTheDocument();
// });

test("sanity check", () => {
  expect(true).toBe(true);
});
